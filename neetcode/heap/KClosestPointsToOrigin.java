package neetcode.heap;

import java.util.*;

/**
 * @author Alex
 * @version 1.0
 * @date 2022/7/11 09:11
 */
public class KClosestPointsToOrigin {
    public int[][] kClosest(int[][] points, int k) {
        //Map<Double, List<Integer>> map = new HashMap<>();
        //Deque<Double> deque = new ArrayDeque<>();
        //for (int i=0; i<points.length; i++){
        //    double dis = Math.sqrt(Math.pow(points[i][0], 2) + Math.pow(points[i][1], 2));
        //    map.put(dis, i);
        //    deque.add(dis);
        //    if (deque.size() > k){
        //        deque.pop();
        //    }
        //}
        //return points[map.get(deque.peek())];
        return points;
    }
}
